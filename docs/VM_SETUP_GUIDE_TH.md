# 🖥️ คู่มือการติดตั้ง Virtual Machine บน Mac M4 (Apple Silicon)

**วัตถุประสงค์:** ติดตั้ง macOS VM บน Mac M4 เพื่อทดสอบ mac-dev-terminal-setup
**อัปเดตล่าสุด:** 2026-01-04
**เป้าหมาย:** Mac M4 (Apple Silicon)

---

## 📋 ภาพรวม

### ทำไมต้องใช้ VM สำหรับการทดสอบ?
- ✅ สภาพแวดล้อมทดสอบที่ปลอดภัย (แยกจากระบบหลัก)
- ✅ Reset/snapshot ได้ง่าย
- ✅ ไม่มีความเสี่ยงต่อระบบหลัก
- ✅ ทดสอบ macOS หลายเวอร์ชันได้
- ✅ ทดสอบหลายสถานการณ์

### ตัวเลือก VM สำหรับ Mac M4 (Apple Silicon):

| ซอฟต์แวร์ | ประเภท | ราคา | ประสิทธิภาพ | ความง่าย | แนะนำ |
|-----------|--------|------|-------------|----------|-------|
| **UTM** | Native | ฟรี | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ ดีที่สุดสำหรับทดสอบ |
| **Parallels Desktop** | Commercial | $99/ปี | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ ดีที่สุดโดยรวม |
| **VMware Fusion** | Commercial | ฟรี/Pro $199 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ✅ ทางเลือกที่ดี |
| **VirtualBox** | N/A | N/A | ❌ | ❌ | ❌ ไม่รองรับ ARM |

---

## 🚀 ตัวเลือก 1: UTM (แนะนำสำหรับการทดสอบฟรี)

### ทำไมต้อง UTM?
- ✅ **ฟรีและ Open Source**
- ✅ รองรับ ARM64 แบบ native
- ✅ ใช้ QEMU (เทคโนโลยีที่พิสูจน์แล้ว)
- ✅ GUI ใช้งานง่าย
- ✅ ประสิทธิภาพดี
- ✅ รองรับ snapshot

### ความต้องการของระบบ:
- macOS 11.0 (Big Sur) หรือใหม่กว่า
- Mac M1/M2/M3/M4 (Apple Silicon)
- RAM อย่างน้อย 8GB (แนะนำ 16GB+)
- พื้นที่ว่างอย่างน้อย 50GB ต่อ VM

---

### ขั้นตอนที่ 1: ติดตั้ง UTM

#### วิธีที่ A: ดาวน์โหลดจากเว็บไซต์
```bash
# 1. เข้าชม
open https://mac.getutm.app/

# 2. ดาวน์โหลด UTM.dmg
# 3. เปิด DMG และลาก UTM ไปที่ Applications
# 4. เปิด UTM
```

#### วิธีที่ B: ติดตั้งผ่าน Homebrew
```bash
# ติดตั้ง UTM
brew install --cask utm

# เปิดโปรแกรม
open -a UTM
```

#### วิธีที่ C: ติดตั้งจาก Mac App Store
- ค้นหา "UTM Virtual Machines"
- ติดตั้ง ($9.99 - สนับสนุนการพัฒนา)
- เหมือนกับเวอร์ชันฟรีทุกประการ

---

### ขั้นตอนที่ 2: ดาวน์โหลด macOS Installer

#### ตัวเลือก A: ดาวน์โหลด Official Installer
```bash
# ติดตั้ง mist-cli (เครื่องมือดาวน์โหลด macOS)
brew install mist-cli

# แสดงรายการ macOS ที่มี
mist list

# ดาวน์โหลด macOS Sonoma (14.x) - แนะนำ
mist download --output-directory ~/Downloads installer "macOS Sonoma"

# หรือดาวน์โหลด macOS Ventura (13.x)
mist download --output-directory ~/Downloads installer "macOS Ventura"

# หรือดาวน์โหลด macOS Sequoia (15.x) - ล่าสุด
mist download --output-directory ~/Downloads installer "macOS Sequoia"
```

**ไฟล์ดาวน์โหลดจะเป็น:** `Install macOS [Version].app` (~12-13GB)

#### ตัวเลือก B: ดาวน์โหลดผ่าน Mac App Store
```bash
# เปิด App Store
open -a "App Store"

# ค้นหา "macOS Sonoma" หรือเวอร์ชันปัจจุบัน
# คลิก "ดู" หรือ "ดาวน์โหลด"
# รอให้ดาวน์โหลดเสร็จ

# Installer จะอยู่ที่:
ls "/Applications/Install macOS"*.app
```

---

### ขั้นตอนที่ 3: สร้าง macOS VM ใน UTM

#### 1. เปิด UTM
```bash
open -a UTM
```

#### 2. สร้าง VM ใหม่
1. คลิก **"+"** (Create a New Virtual Machine)
2. เลือก **"Virtualize"** (สำหรับ ARM64 macOS)
3. เลือก **"macOS 12+"**

#### 3. ตั้งค่า VM

**Boot ISO Image:**
- Browse ไปที่: `/Applications/Install macOS Sonoma.app`
- หรือ installer ที่ดาวน์โหลดมา

**การตั้งค่า Hardware:**
- **RAM:** 8192 MB (8GB) ขั้นต่ำ, 16384 MB (16GB) แนะนำ
- **CPU Cores:** 4 cores ขั้นต่ำ, 6-8 cores แนะนำ
- **Storage:** 50 GB ขั้นต่ำ, 100 GB แนะนำ

**การตั้งค่าขั้นสูง:**
- ✅ เปิด "Use Apple Virtualization"
- ✅ เปิด "Enable hardware OpenGL acceleration"

#### 4. สร้าง VM
- คลิก **"Save"**
- UTM จะสร้าง VM

---

### ขั้นตอนที่ 4: ติดตั้ง macOS

#### 1. เริ่ม VM
- เลือก VM ของคุณใน UTM
- คลิกปุ่ม **▶️ Play**

#### 2. กระบวนการติดตั้ง macOS
1. **เลือกภาษา**
   - เลือกภาษาของคุณ
   - คลิก Continue

2. **Disk Utility**
   - เลือก "Disk Utility"
   - เลือก "QEMU HARDDISK" หรือ "Apple Disk Image"
   - คลิก "Erase"
   - ชื่อ: "Macintosh HD"
   - รูปแบบ: "APFS (Encrypted)" หรือ "APFS"
   - คลิก "Erase"
   - ออกจาก Disk Utility

3. **ติดตั้ง macOS**
   - เลือก "Install macOS"
   - Continue
   - ยอมรับข้อตกลง
   - เลือก "Macintosh HD"
   - รอการติดตั้ง (30-60 นาที)

4. **Setup Assistant**
   - ภูมิภาค: ภูมิภาคของคุณ
   - ภาษา: ภาษาของคุณ
   - การเข้าถึง: ข้าม
   - ข้อมูลและความเป็นส่วนตัว: Continue
   - Migration Assistant: "ไม่ใช่ตอนนี้"
   - **Apple ID: ข้าม** (ไม่จำเป็นสำหรับการทดสอบ)
   - ข้อตกลง: ยอมรับ
   - บัญชีคอมพิวเตอร์:
     - ชื่อเต็ม: "Test User"
     - ชื่อบัญชี: "test"
     - รหัสผ่าน: "test" (ง่ายสำหรับการทดสอบ)
   - Express Set Up: Continue
   - Analytics: ไม่แชร์
   - Screen Time: ตั้งค่าภายหลัง
   - Siri: ข้าม
   - เลือกธีม: Light/Dark/Auto

5. **ติดตั้งเสร็จสมบูรณ์**
   - macOS จะโหลดไปที่ desktop
   - การติดตั้งเสร็จสมบูรณ์! 🎉

**เวลาติดตั้ง:** 30-60 นาที รวมทั้งหมด

---

### ขั้นตอนที่ 5: การตั้งค่าหลังติดตั้ง

#### 1. ติดตั้ง Xcode Command Line Tools (จำเป็น)
```bash
# สิ่งนี้สำคัญมากสำหรับ mac-dev-terminal-setup
sudo xcode-select --install
```

#### 2. เปิด Guest Account (ตัวเลือก - สำหรับ reset ง่าย)
```bash
# System Settings → Users & Groups → Add User
# เลือก "Guest User"
```

#### 3. ปรับการตั้งค่า VM (ตัวเลือก)
```bash
# ใน UTM, หยุด VM ก่อน:
# คลิก "Edit" (ไอคอนดินสอ)

# ปรับตามต้องการ:
# - RAM (ถ้ามีมากขึ้น)
# - CPU cores
# - ความละเอียดจอ
```

#### 4. สร้าง Snapshot (แนะนำ)
```bash
# ใน UTM:
# คลิก "⋯" (จุดสามจุด) ข้าง VM
# เลือก "Snapshot"
# ชื่อ: "Fresh Install - Before Testing"

# สามารถ restore กลับไปที่ snapshot นี้ได้ทุกเมื่อ
```

---

### ขั้นตอนที่ 6: ทดสอบ mac-dev-terminal-setup

#### 1. เริ่ม VM
```bash
# ใน UTM, คลิก ▶️ Play
# รอให้ macOS boot
```

#### 2. เปิด Terminal
```bash
# Applications → Utilities → Terminal
# หรือกด: Cmd + Space, พิมพ์ "Terminal"
```

#### 3. ดาวน์โหลดและรัน Script
```bash
# ดาวน์โหลด script
curl -fsSL https://raw.githubusercontent.com/thaicyber/mac-dev-terminal-setup/main/install.sh -o ~/install.sh

# ตรวจสอบ script (ตัวเลือก)
cat ~/install.sh

# รันการติดตั้ง
bash ~/install.sh

# ทำตามขั้นตอนการติดตั้ง
# ทดสอบทุก optional groups
```

#### 4. รันการทดสอบ
```bash
# ทำตาม TESTING_CHECKLIST_TH.md
# ตรวจสอบ components ทั้งหมด

# ทดสอบอย่างรวดเร็ว:
source ~/.zshrc
aliashelp
nvm list
brew list
```

---

### ขั้นตอนที่ 7: การจัดการ Snapshot

#### สร้าง Snapshot หลังการติดตั้ง
```bash
# ใน UTM:
# หยุด VM หรือ Save State
# คลิก "⋯" → "Snapshot"
# ชื่อ: "V7 Installed - All Tools"
```

#### Restore Snapshot
```bash
# ใน UTM:
# คลิก "⋯" → "Snapshots"
# เลือก snapshot
# คลิก "Restore"
```

#### ลบ Snapshot
```bash
# ใน UTM:
# คลิก "⋯" → "Snapshots"
# เลือก snapshot
# คลิก "Delete"
```

---

### เคล็ดลับ & ลูกเล่น UTM

#### การปรับ Performance:
```bash
# 1. จัดสรร RAM มากขึ้น (ถ้ามี)
# 2. จัดสรร CPU cores มากขึ้น
# 3. เปิด hardware acceleration
# 4. ใช้ APFS สำหรับรูปแบบ disk
# 5. ปิดแอปที่ไม่จำเป็นบน host Mac
```

#### การแชร์ Clipboard:
```bash
# UTM ไม่รองรับ clipboard sharing โดยค่าเริ่มต้น
# วิธีแก้: ใช้การแชร์ไฟล์หรือเครือข่าย

# เปิดการแชร์ไฟล์:
# macOS VM → System Settings → Sharing → File Sharing
```

#### การตั้งค่าเครือข่าย:
```bash
# UTM ใช้ "Shared Network" โดยค่าเริ่มต้น
# VM สามารถเข้าถึงอินเทอร์เน็ต
# VM ไม่สามารถเข้าถึงได้จาก host (เพื่อความปลอดภัย)

# ทดสอบเครือข่าย:
ping 8.8.8.8
curl https://google.com
```

#### ความละเอียดจอ:
```bash
# ปรับใน VM:
# System Settings → Displays → Resolution
# เลือกความละเอียดที่ปรับ

# หรือในการตั้งค่า UTM (VM ต้องหยุด):
# Edit → Display → Resolution
```

---

## 🚀 ตัวเลือก 2: Parallels Desktop (ประสิทธิภาพดีที่สุด)

### ทำไมต้อง Parallels?
- ✅ **ประสิทธิภาพดีที่สุด** บน Apple Silicon
- ✅ **บูรณาการอย่างราบรื่น** กับ macOS
- ✅ Coherence mode (รันแอป Windows/Linux เหมือน native)
- ✅ รองรับ USB device
- ✅ แชร์ clipboard
- ✅ แชร์ไฟล์
- ❌ **เสียเงิน** ($99/ปี สำหรับ Standard, $119/ปี สำหรับ Pro)

### ความต้องการของระบบ:
- macOS 11.0 หรือใหม่กว่า
- Mac M1/M2/M3/M4
- RAM 8GB ขั้นต่ำ (แนะนำ 16GB+)
- พื้นที่ว่าง 50GB ต่อ VM

---

### ขั้นตอนที่ 1: ติดตั้ง Parallels Desktop

#### ดาวน์โหลดและติดตั้ง:
```bash
# เข้าชม
open https://www.parallels.com/products/desktop/

# ดาวน์โหลด trial หรือซื้อ
# Trial: ฟรี 14 วัน

# ติดตั้ง:
# 1. ดาวน์โหลด Parallels Desktop.dmg
# 2. เปิด DMG
# 3. Double-click "Install Parallels Desktop"
# 4. ทำตาม installation wizard
# 5. ใส่ license key (หรือเริ่ม trial)
```

---

### ขั้นตอนที่ 2: สร้าง macOS VM

#### 1. เปิด Parallels
```bash
open -a "Parallels Desktop"
```

#### 2. สร้าง VM ใหม่
1. File → New...
2. เลือก **"Install Windows or another OS from a DVD or image file"**
3. คลิก Continue
4. เลือก **"Install macOS using the Recovery Partition"**
   - หรือ: "Choose manually" → Browse ไปหา macOS installer

#### 3. ชื่อและตำแหน่ง
- ชื่อ: "macOS Test VM"
- ตำแหน่ง: Default (หรือเลือกเอง)
- ✅ สร้าง alias บน Desktop (ตัวเลือก)

#### 4. การตั้งค่า (ก่อน starting)
- คลิก **"Configure"** ก่อน starting
- **Hardware:**
  - Processors: 4 CPUs (หรือมากกว่า)
  - Memory: 8192 MB (หรือมากกว่า)
  - Graphics: 2048 MB
- **Hard Disk:**
  - ขยายเป็น 100 GB (แนะนำ)

---

### ขั้นตอนที่ 3: ติดตั้ง macOS

คล้ายกับกระบวนการ UTM (ดู UTM ขั้นตอนที่ 4)

**ข้อดีใน Parallels:**
- การติดตั้งเร็วกว่า
- ติดตั้ง tools อัตโนมัติ
- ประสิทธิภาพ graphics ดีกว่า

---

### ขั้นตอนที่ 4: ติดตั้ง Parallels Tools

**หลังการติดตั้ง macOS:**
```bash
# Parallels จะถามให้ติดตั้ง Parallels Tools
# คลิก "Install"
# หรือ: Actions → Install Parallels Tools...

# ประโยชน์:
# - Graphics ดีขึ้น
# - แชร์ clipboard
# - Drag & drop ไฟล์
# - เข้าถึง USB device
```

---

### ขั้นตอนที่ 5: ตั้งค่าสภาพแวดล้อมทดสอบ

เหมือนกับ UTM (ขั้นตอนที่ 6 ด้านบน)

---

### เคล็ดลับ & ลูกเล่น Parallels

#### Coherence Mode:
```bash
# View → Enter Coherence
# แอป VM ปรากฏใน macOS
# ประสบการณ์ที่ราบรื่น

# ออก: View → Exit Coherence
```

#### Shared Folders:
```bash
# Configure → Options → Sharing
# เปิด "Share Mac folders with Linux/Windows"

# ใน VM:
# Finder → Locations → "Parallels Shared Folders"
```

#### Snapshots:
```bash
# สร้าง snapshot:
# Actions → Take Snapshot...
# ชื่อ: "Before Testing"

# Restore:
# Actions → Manage Snapshots...
# เลือก snapshot → Restore
```

---

## 🚀 ตัวเลือก 3: VMware Fusion (ทางเลือกที่ดี)

### ทำไมต้อง VMware Fusion?
- ✅ ฟรีสำหรับการใช้งานส่วนตัว (ตั้งแต่ 2024)
- ✅ ฟีเจอร์ระดับมืออาชีพ
- ✅ ประสิทธิภาพดีบน Apple Silicon
- ✅ มาตรฐานอุตสาหกรรม
- ❌ ซับซ้อนกว่า Parallels เล็กน้อย

### การติดตั้ง:

```bash
# ดาวน์โหลด
open https://www.vmware.com/products/fusion/fusion-evaluation.html

# สร้างบัญชี (ฟรี)
# ดาวน์โหลด VMware Fusion สำหรับ Apple Silicon

# ติดตั้ง:
# 1. เปิด DMG
# 2. ลากไปที่ Applications
# 3. เปิดและยอมรับ license
```

### การสร้าง macOS VM:

กระบวนการคล้ายกับ Parallels:
1. File → New
2. Install from disc or image
3. เลือก macOS installer
4. ตั้งค่า resources
5. ติดตั้ง macOS

---

## 📊 เปรียบเทียบประสิทธิภาพ

### ผลการทดสอบ (Mac M4 พร้อม RAM 32GB):

| ตัววัด | UTM | Parallels | VMware Fusion |
|--------|-----|-----------|---------------|
| **เวลา Boot** | ~30s | ~20s | ~25s |
| **การติดตั้ง macOS** | 40-50min | 30-40min | 35-45min |
| **RAM Overhead** | ~1GB | ~500MB | ~800MB |
| **Graphics** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **USB Support** | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **File Sharing** | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Clipboard** | ❌ | ✅ | ✅ |
| **Snapshots** | ✅ | ✅ | ✅ |

---

## 🎯 ข้อเสนอแนะเวิร์กโฟลว์การทดสอบ

### เวิร์กโฟลว์ 1: ทดสอบอย่างรวดเร็ว (UTM)
```
1. สร้าง VM ใหม่จาก snapshot
2. รัน install.sh
3. ทำตาม TESTING_CHECKLIST_TH.md
4. บันทึกปัญหา
5. Restore snapshot (reset)
6. ทำซ้ำ
```

**เวลาต่อรอบ:** 45-60 นาที

---

### เวิร์กโฟลว์ 2: ทดสอบอย่างครอบคลุม (Parallels/VMware)
```
1. สร้าง VM ใหม่
2. ติดตั้ง macOS
3. สร้าง snapshot: "Fresh macOS"
4. ติดตั้ง mac-dev-terminal-setup
5. ทดสอบเต็มรูปแบบ (ตอบ Y ทั้งหมด)
6. สร้าง snapshot: "Full Installation"
7. ทดสอบแต่ละฟีเจอร์
8. ทดสอบ uninstall
9. Restore ไปที่ "Fresh macOS"
10. ทดสอบการติดตั้งแบบต่างๆ
```

**เวลาต่อรอบ:** 2-3 ชั่วโมงสำหรับการทดสอบอย่างครอบคลุม

---

### เวิร์กโฟลว์ 3: หลายสถานการณ์
```
สถานการณ์ A: การติดตั้งขั้นต่ำ
- Core เท่านั้น, ข้ามทั้งหมด optional

สถานการณ์ B: Developer Setup
- Core + Node.js + Developer Tools

สถานการณ์ C: DevOps Setup
- Core + Developer + DevOps + Cloud

สถานการณ์ D: การติดตั้งเต็ม
- ทุกอย่าง (ตอบ Y ทั้งหมด)
```

---

## 🐛 ปัญหา VM ที่พบบ่อย & วิธีแก้ไข

### ปัญหา 1: VM ไม่ start
**วิธีแก้:**
```bash
# ตรวจสอบ:
# 1. จัดสรร RAM เพียงพอ?
# 2. พื้นที่ disk เพียงพอ?
# 3. เวอร์ชัน macOS เข้ากันได้?

# ลอง:
# - ลด RAM allocation
# - เพิ่มพื้นที่ disk บน host
# - อัปเดตซอฟต์แวร์ VM
```

---

### ปัญหา 2: ประสิทธิภาพช้า
**วิธีแก้:**
```bash
# ปรับให้เหมาะสม:
# 1. จัดสรร RAM มากขึ้น (แนะนำ 16GB)
# 2. จัดสรร CPU cores มากขึ้น (4-6)
# 3. เปิด hardware acceleration
# 4. ปิดแอปอื่นบน host
# 5. ใช้ SSD สำหรับจัดเก็บ VM
```

---

### ปัญหา 3: เครือข่ายไม่ทำงาน
**วิธีแก้:**
```bash
# ใน VM:
# System Settings → Network
# ตรวจสอบการเชื่อมต่อ

# ทดสอบ:
ping 8.8.8.8
curl https://google.com

# Restart เครือข่าย:
sudo ifconfig en0 down
sudo ifconfig en0 up
```

---

### ปัญหา 4: การติดตั้งล้มเหลว
**วิธีแก้:**
```bash
# สาเหตุทั่วไป:
# 1. พื้นที่ disk ไม่พอ
# 2. Installer เสียหาย
# 3. เครือข่ายขาด

# แก้ไข:
# - ขยายขนาด VM disk
# - ดาวน์โหลด installer ใหม่
# - ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
```

---

## 📚 แหล่งข้อมูลเพิ่มเติม

### เอกสาร:
- **UTM:** https://docs.getutm.app/
- **Parallels:** https://kb.parallels.com/
- **VMware Fusion:** https://docs.vmware.com/en/VMware-Fusion/

### ชุมชน:
- UTM GitHub: https://github.com/utmapp/UTM
- Parallels Forums: https://forum.parallels.com/
- VMware Forums: https://communities.vmware.com/

### macOS Installers:
- mist-cli: https://github.com/ninxsoft/mist-cli
- Apple: https://support.apple.com/en-us/HT211683

---

## 💡 แนวปฏิบัติที่ดี

### 1. Snapshot เสมอก่อนทดสอบ
```bash
# สร้าง snapshots ที่จุดสำคัญ:
# - การติดตั้ง macOS สด
# - ก่อนรัน script
# - หลังการติดตั้งสำเร็จ
# - ก่อน uninstall
```

### 2. บันทึกทุกอย่าง
```bash
# เก็บ log การทดสอบ:
# - ทดสอบอะไร
# - ผลลัพธ์ (pass/fail)
# - ปัญหาที่พบ
# - ขั้นตอนทำซ้ำ
```

### 3. ทดสอบหลายสถานการณ์
```bash
# อย่าทดสอบแค่ happy path:
# - ทดสอบกับ errors (ไม่มีอินเทอร์เน็ต)
# - ทดสอบกับการติดตั้งที่มีอยู่
# - ทดสอบโหมด reinstall
# - ทดสอบโหมด uninstall
```

### 4. ใช้ระบบอัตโนมัติ (ถ้าเป็นไปได้)
```bash
# สร้าง test scripts:
# - การติดตั้งอัตโนมัติ
# - การตรวจสอบอัตโนมัติ
# - การทำความสะอาดอัตโนมัติ
```

---

## 🎯 สรุป

### การตั้งค่าที่แนะนำสำหรับทดสอบ mac-dev-terminal-setup:

**สำหรับการทดสอบฟรี:**
- ✅ ใช้ **UTM**
- สร้างหลาย snapshots
- ทดสอบทุกสถานการณ์
- ~2-3 ชั่วโมง รวมการตั้งค่า

**สำหรับการทดสอบมืออาชีพ:**
- ✅ ใช้ **Parallels Desktop** (trial หรือซื้อ)
- ประสิทธิภาพดีกว่า
- บูรณาการดีกว่า
- รอบการทดสอบเร็วขึ้น

**สำหรับทั้งสอง:**
- ทำตาม TESTING_CHECKLIST_TH.md
- บันทึกการค้นพบทั้งหมด
- สร้างรายงานการทดสอบอย่างครอบคลุม

---

**เวอร์ชัน:** V7
**เอกสาร:** VM_SETUP_GUIDE_TH.md
**Repository:** https://github.com/thaicyber/mac-dev-terminal-setup

